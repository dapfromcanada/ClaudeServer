[project]
name = "mcp-filesystem-server"
version = "0.1.0"
description = "MCP server providing filesystem and bash access to Claude"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "anthropic>=0.39.0",
    "PyQt6>=6.6.0",
]

[dependency-groups]
dev = []
